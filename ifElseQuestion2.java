package com.edgetechHomeWork;

import org.w3c.dom.ls.LSOutput;

public class ifElseQuestion2 {
    public static void main(String[] args) {
        /*Ask the user for a number less than or equal to 5. Now, using if statements,
        create a code to print a string with information
        about the number. Some numbers will match multiple criteria. You need to return
        all the information for that number.
         */
       int number = '5';
       //is the int above correct? Do i need it to == to something?
        //if (number>=6)
        if (number<=5) {
           System.out.println("Not Elected");
       }else System.out.println("Move Forward");
        System.out.println(number=2);
       }




    }

